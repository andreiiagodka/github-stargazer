import React from 'react'

import Header from './Header'
import Content from './Content'
import Layout from '../UI/Layout'

const AddRepositoryPage = () => {
  return <Layout header={<Header />} body={<Content />} />
}

export default AddRepositoryPage
