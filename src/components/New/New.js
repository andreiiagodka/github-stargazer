import React, { Fragment } from "react";

import Header from '../UI/Header/Header';
import Body from '../../containers/New/Body/Body';

const addRepository = () => (
  <Fragment>
    <Header title='Add Repository' />
    <Body />
  </Fragment>
)

export default addRepository